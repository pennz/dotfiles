#!/bin/bash -li

# config change commit
cb=$(config rev-parse --abbrev-ref HEAD)
config stash; config checkout master; 
config pull && \
config stash pop
( [ $# -lt 1 ] || config commit -asm "$*" ) && \
config push; config checkout $cb && \
config merge --no-edit master
